export const PORT = process.env.PORT || 5555; // Default to 5555 if PORT isn't set in the environment
export const MONGO_DB_URI = process.env.MONGO_DB_URI; // Use the environment variable for MongoDB URL
